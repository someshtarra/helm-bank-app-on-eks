===============================================================================
 BANK APPLICATION (AWS EKS) - DEPLOYED SUCCESSFULLY!
===============================================================================

1. Get the application URL by running these commands:
{{- if contains "LoadBalancer" .Values.service.type }}
  NOTE: It may take a few minutes for the AWS Load Balancer to be provisioned.
  You can watch the status of the service by running:
    kubectl get --namespace {{ .Release.Namespace }} svc -w {{ include "bank-app.fullname" . }}

  Export the Load Balancer hostname:
    export SERVICE_IP=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "bank-app.fullname" . }} --template "{{"{{ range (index .status.loadBalancer.ingress 0) }}{{.}}{{ end }}"}}")
    echo "Bank Application URL: http://$SERVICE_IP:{{ .Values.service.port }}"
{{- else if contains "ClusterIP" .Values.service.type }}
  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "{{ include "bank-app.selectorLabels" . }}" -o jsonpath="{.items[0].metadata.name}")
  kubectl --namespace {{ .Release.Namespace }} port-forward $POD_NAME 8080:{{ .Values.service.targetPort }}
  echo "Bank Application URL: http://127.0.0.1:8080"
{{- end }}

2. Verify Pod Health and Autoscaling Status:
    kubectl get pods -n {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "bank-app.name" . }}"
    kubectl get hpa -n {{ .Release.Namespace }}

3. Check Persistent Storage and Claim Binding:
    kubectl get pvc,pv -n {{ .Release.Namespace }}

4. Verify RBAC and ServiceAccount Configuration:
    kubectl get sa,role,rolebinding -n {{ .Release.Namespace }}

===============================================================================
 Architectural Alert: AWS EBS ReadWriteOnce (RWO) Notice:
 If HPA scales pods across different nodes, ensure you review docs/architecture.md
 regarding multi-node EBS attachment constraints and AWS EFS migration options.
===============================================================================
